---
title: Slash
description: Cross-platform application JUST for messaging.
---


## Introduction

Chat – a simple word for a simple app. No nuclear collider of pointless, useless features here. As a developer who thinks WordPress is shit and would rather build something fresh from the ground several times, I wanted an app that’s as straightforward as its name.

C – Cross-platform:
I'm not a tech-giant to have time and money to batter with one button looking the same on different devices and platforms. So... we have choice: React-Native. That is not just a web-view (based on MTAA requirements), JS ❤️, and good, excellent ecosystem.

H – Huge Performance:
I'm a programmer, and if my system doesn’t boot in a second, it’s bullshit. Sure, React Native might throw a wrench in the works sometimes, but in good hands and with a little clever tinkering it’s fast as hell (don't know why hell is fast). And on the backend, we’re using Bun to keep message sending lightning quick.

A – Atomicity:
When millions of messages are flying around, reliability is non-negotiable. With Bun and Elysia (plus their awesome plugins), every message is handled solidly, with logs and stats that actually mean something.

T – Touch-Friendly:
I don't like when mobile application are designed like I am using the phone with two hands. **I CANNOT CLICK ON LEFT TOP CORNER BUTTON**. That is even better on iphoneos that does not support gestures, and you need to touch in that corner several times.

So... Simple, Fast, Atomic, and (most importantly) Easy to use.

### Authors

<Cards>
    <Card
        title="Artur Kozubov" href="https://github.com/justAnArthur"
        description="Billionaire, genius, playboy and philanthropist."
        icon={
            <img src="https://avatars.githubusercontent.com/u/88618060?v=4" width={25} height={25} alt="Artur Kozubov"/>
        }
    />
    <Card
        title="Artem Zaitsev" href="https://github.com/Aldeimeter"
        description="*Where is your avatar?"
        icon={
            <img src="https://avatars.githubusercontent.com/u/114435500?v=4" width={25} height={25}
                 alt="Artem Zaitsev"/>
        }
    />
</Cards>

## Tech Overview

### Tech stack

<div className="grid grid-cols-5 gap-4">
    <div className="col-span-3 col-start-2 row-start-2 card p-4 flex flex-col items-center justify-center">
        <img src="https://reactnative.dev/img/header_logo.svg" alt="React Native" width={64} height={64}/>
        <b>React Native</b>
        <div className="text-center text-xs !m-0">
            Is a JavaScript framework for writing real, natively rendering mobile applications for iOS and Android, Web.
        </div>
    </div>

    <div className="col-start-3 row-start-3 col-span-2 card p-4 flex flex-col items-center">
        <img src="/tech-logos/expo-wordmark-light.png" alt="Expo" width={128} height={36.125}/>
        <b style={{display: "hidden"}}>Expo</b>
        <div className="text-center text-xs !m-0">
            Is an additional framework for React Native that allows you to build, deploy, and publish applications
            without pain.
        </div>
    </div>

    <div className="col-span-3 card p-4 flex flex-col items-center justify-center">
        <img src="https://elysiajs.com/assets/elysia.svg" alt="Elysia" width={64} height={64}/>
        <b>Elysia</b>
        <div className="text-center text-xs !m-0">
            TypeScript framework supercharged by Bun with End-to-End Type Safety, unified type system, and outstanding
            developer experience.
        </div>
    </div>

    <div className="col-start-1 row-start-2 col-span-1 card p-4 flex flex-col items-center justify-center">
        <img src="https://www.clipartmax.com/png/full/79-796772_tree-logo-design-vector-png-tree-vector-logo-png.png"
             alt="Elysia/Eden" width={64} height={64}/>
        <b>Elysia/Eden</b>
        <div className="text-center text-xs !m-0">
            End-to-end type safety is like making sure all the pieces of the track match up correctly so the train
            doesn't fall off or get stuck.
        </div>
    </div>

    <div className="col-start-5 row-start-2 col-span-1 card p-4 flex flex-col items-center justify-center">
        <img
            src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fbun.sh%2Flogo-square.png&f=1&nofb=1&ipt=749187b73ff66c12171f7ef2eb62ed6747a626a2347b57139a8e583347634491&ipo=images"
            alt="Bun" width={64} height={64}/>
        <b>Bun</b>
        <div className="text-center text-xs !m-0">
            Is an all-in-one toolkit for JavaScript and TypeScript apps.
        </div>
    </div>

    <div className="col-span-2 col-start-4 row-start-1 card p-4 flex flex-col items-center justify-center">
        <img className="rounded-lg"
             src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Ftse2.mm.bing.net%2Fth%3Fid%3DOIP.fiSV5UApVkxBw4eg0XQMIQAAAA%26pid%3DApi&f=1&ipt=25af8b3e2126b08fffca68ff0c60210123e4d6d2305b8040148af3b9d8034a03&ipo=images"
             alt="Drizzle" width={64} height={64}/>
        <b>Drizzle</b>
        <div className="text-center text-xs !m-0">
            It’s the only ORM with both relational and SQL-like query APIs. Is lightweight, performant, typesafe,
            non-lactose, gluten-free, sober, flexible and serverless-ready by design.
        </div>
    </div>

    <div className="col-start-5 row-start-3 col-span-1 card p-4 flex flex-col items-center justify-center">
        <b>i18n-js</b>
        <div className="text-center text-xs !m-0">
            A small library to provide the i18n translations on the JavaScript.
        </div>
    </div>

    <div className="col-span-2 card p-4 flex flex-col items-center">
        <img
            src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fwww.typedoc-plugin-markdown.org%2F_next%2Fimage%3Furl%3D%252Flogos%252Ftypedoc-logo.png%26w%3D64%26q%3D75&f=1&nofb=1&ipt=3356e5e627e7572096d5636d786aba313434eafd64a4f6d958efdc1f4fb98f7f&ipo=images"
            alt="Typedoc" width={64} height={64}/>
        <b>Typedoc</b>
        <div className="text-center text-xs !m-0">
            Is a documentation generator for TypeScript projects.
        </div>
    </div>
</div>

### Architecture

Based on requirements and our experience, we decided to use the following architecture:

```mermaid
flowchart RL
  subgraph Clients
    subgraph Web_Client
        A[🖥 Web Client]
        A1["📂 Local Storage DB"]
        A2["Web browser's APIs"]
    end
    subgraph Android_Client
        B[🤖 Android Client]
        B1["📂 Local Simple DB (Android)"]
        B2["Expo APIs"]
    end
    subgraph iOS_Client_Optional
        C["🍏 iOS Client (Optional)"]
        C1["📂 Local DB (iOS)"]
        C2["Expo APIs"]
    end
  end

  A --> A1
  B --> B1
  C --> C1
  A <--> A2
  B <--> B2
  C <--> C2

  subgraph "VPS (Europe)"
    LB[🛡 Load Balancers]
    BS[Bun Server Node]

    DB[(🗄️ Postgres DB)]
  end

  A -- "Sync / Send Messages" --> LB
  B -- "Sync / Send Messages" --> LB
  C -- "Sync / Send Messages" --> LB

  LB --> BS

  BS -- "Persistent Data Ops" --> DB
```

#### Database Architecture

##### Entity-Relationship diagram

```mermaid
erDiagram
    USER {
      text id PK
      text name
      text email
      boolean emailVerified
      text image
      timestamp createdAt
      timestamp updatedAt
    }

    SESSION {
      text id PK
      text userId FK
      text token
      timestamp expiresAt
      text ipAddress
      text userAgent
      timestamp createdAt
      timestamp updatedAt
    }

    ACCOUNT {
      text id PK
      text userId FK
      text accountId
      text providerId
      text accessToken
      text refreshToken
      timestamp accessTokenExpiresAt
      timestamp refreshTokenExpiresAt
      text scope
      text password
      timestamp createdAt
      timestamp updatedAt
    }

    VERIFICATION {
      text id PK
      text identifier
      text value
      timestamp expiresAt
      timestamp createdAt
      timestamp updatedAt
    }

    PRIVATE_CHAT {
      text id PK
      text user1Id FK
      text user2Id FK
    }

    GROUP_CHAT {
      text id PK
      text name
      text createdBy FK
      timestamp createdAt
    }

    GROUP_CHAT_MEMBER {
      text id PK
      text groupChatId FK
      text userId FK
      timestamp joinedAt
    }

    MESSAGE {
      text id PK
      timestamp createdAt
      text senderId FK
      text chatId FK "can reference PRIVATE_CHAT.id or GROUP_CHAT.id"
      text content
      text type
    }

    %% Relationships
    SESSION ||--|| USER : "belongs to"
    ACCOUNT ||--|| USER : "belongs to"
    PRIVATE_CHAT }|..|{ USER : "user1Id, user2Id"
    GROUP_CHAT }|..|| USER : "createdBy"
    GROUP_CHAT_MEMBER }|--|| GROUP_CHAT : "member of"
    GROUP_CHAT_MEMBER }|--|| USER : "member"
    MESSAGE }|--|| USER : "sender"
```

##### Class diagram

```mermaid
classDiagram
    class User {
      <<entity>>
      - id: text
      - name: text
      - email: text
      - emailVerified: boolean
      - image: text
      - createdAt: timestamp
      - updatedAt: timestamp
    }

    class Session {
      <<entity>>
      - id: text
      - userId: text
      - token: text
      - expiresAt: timestamp
      - ipAddress: text
      - userAgent: text
      - createdAt: timestamp
      - updatedAt: timestamp
    }

    class Account {
      <<entity>>
      - id: text
      - userId: text
      - accountId: text
      - providerId: text
      - accessToken: text
      - refreshToken: text
      - accessTokenExpiresAt: timestamp
      - refreshTokenExpiresAt: timestamp
      - scope: text
      - password: text
      - createdAt: timestamp
      - updatedAt: timestamp
    }

    class Verification {
      <<entity>>
      - id: text
      - identifier: text
      - value: text
      - expiresAt: timestamp
      - createdAt: timestamp
      - updatedAt: timestamp
    }

    class PrivateChat {
      <<entity>>
      - id: text
      - user1Id: text
      - user2Id: text
    }

    class GroupChat {
      <<entity>>
      - id: text
      - name: text
      - createdBy: text
      - createdAt: timestamp
    }

    class GroupChatMember {
      <<entity>>
      - id: text
      - groupChatId: text
      - userId: text
      - joinedAt: timestamp
    }

    class Message {
      <<entity>>
      - id: text
      - createdAt: timestamp
      - senderId: text
      - chatId: text  "references PrivateChat OR GroupChat"
      - content: text
      - type: text    "e.g., TEXT, IMAGE, GPS"
    }

    %% Associations
    Session --> User : userId
    Account --> User : userId
    PrivateChat --> User : user1Id
    PrivateChat --> User : user2Id
    GroupChat --> User : createdBy
    GroupChatMember --> GroupChat : groupChatId
    GroupChatMember --> User : userId
    Message --> User : senderId
    %% Note: Message.chatId may refer to either a PrivateChat or a GroupChat
```

## Project Overview

### Pointed platforms

#### Front-end

Based on possibilities of React Native, we can build applications for iOS, **Android**, and **Web**. Due to limitation of non-using and negative experience about iOS/iPhones we are not sure about shipping iOS build.

So primary we are focusing on stable Android and Web builds, with optional iOS.

#### Back-end

The back-end is built on Elysia, which is a TypeScript framework supercharged by Bun. With possibilities of Bun - it can be built to binary and run on every machine.

Primary, production target is **Linux**, and developing include **Windows**.

### Use cases

#### Diagram based

```mermaid
flowchart LR
    %% actor
    U[User]

    subgraph Customization["Customization & Notifications"]
        CN1[Change Theme]
        CN2[Dark Mode]
        CN3[Light Mode]
        CN4[High Contrast Mode]
        CN5[Change Chat Notification Settings]
    end

    subgraph AccountManagement["Account Management"]
        AM1[Register Account]
        AM2[Login]
        AM3[Logout]
        AM4[Manage Profile]
        AM5[Change Avatar]
        AM7[Optional Two-Factor Authentication]
    end

    subgraph ChatManagement["Chat Creation & Management"]
        CMg1[Create Chat with Someone]
        CMg2[Create Group Chat]
        CMg3[Add Users]
        CMg5["View Chat History (Messages with infinity scroll)"]
    end

    subgraph Messaging["Messaging"]
        M1[Send Message]
        M2[Send Text Message]
        M3[Send Image from Camera]
        M4[Send Image from Gallery]
        M5[Send Geolocation]
        M8[Delete Message]
    end

    U --- AM1
    U --- AM2
    U --- AM3
    U --- AM4
    U --- AM7

    AM4 --- AM5
    AM4 --- AM7
    AM2 --- AM7

    U --- CMg1
    U --- CMg2
    CMg2 --- CMg3
    U --- CMg5

    U --- M1
    M1 --- M2
    M1 --- M3
    M1 --- M4
    M1 --- M5
    M1 --- M8

    U --- CN1
    CN1 --- CN2
    CN1 --- CN3
    CN1 --- CN4
    U --- CN5

    classDef actorStyle fill:#f9f,stroke:#333,stroke-width:2px;
    class U actorStyle;
```

#####  Authorization

```mermaid
flowchart LR
    TRIGGER([User requests to register/login])
    style TRIGGER fill:#cfc
    TRIGGER --> S1["System displays combined login/registration screen<br/>(email &amp; password)"]
    S1 --> S2["User enters credentials and selects 'Login' or 'Register'"]
    S2 --> S3[System checks backend for account existence]
    S3 --> S4{Does the account exist?}
    S4 -- Yes --> S5[Proceed with login]
    S4 -- No --> S6[Register new user]
    S6 --> S7[User registered successfully]
    S5 & S7 --> S8[Backend returns authentication success]
```

Optional Two-Factor Authentication:

```mermaid
flowchart LR
    S8[Backend returns authentication success]
    S8 --> OPT{User opts for 2FA?}
    OPT -- Yes --> S9[Backend sends 2FA code to user's device]
    S9 --> S10[User enters the 2FA code]
    S10 --> S11[System verifies the 2FA code]
    S11 -- Valid --> SUCCESS([Login successful])
    S11 -- Invalid --> EXT1[Notify user: invalid 2FA code, please retry] --> S10
    OPT -- No --> SUCCESS
    style SUCCESS fill:#cfc
```

##### Chat message sending

```mermaid
flowchart LR
  %% Trigger event: User selects a chat
  TRIGGER([User selects a chat]) --> CHOICE;
  style TRIGGER fill:#ccf

  %% User chooses action: text or attachment
  CHOICE[User chooses action:<br>- Enter text message<br>- Click attachment button] --> DECIDE;

  %% Decision: is a text message entered?
  DECIDE{Did the user enter a text message?}

  %% Text Message Flow
  DECIDE -- Yes --> SENDTEXT["User clicks 'Send' for text message"]
  SENDTEXT --> LOCALSYNC[Text message saved in local storage]
  LOCALSYNC --> SENDSERVER[Text message sent to server]
  SENDSERVER --> CHECKTEXT{Was text message sent successfully?}
  CHECKTEXT -- Yes --> TEXTSUCCESS[Text message delivered]
  CHECKTEXT -- No --> RETRYTEXT[Prompt user to retry sending text message]

  %% Attachment Flow
  DECIDE -- No --> ATTACHFLOW[User clicks attachment button]
  ATTACHFLOW --> CHOOSEATTACH[User selects attachment type]

  %% Attachment Options: Camera, Gallery, or Geo-location
  CHOOSEATTACH --> CAMERA[Select image from camera]
  CHOOSEATTACH --> GALLERY[Select image from gallery]
  CHOOSEATTACH --> GEO[Select current geo location]

  %% Preview before sending attachment
  CAMERA --> PREVIEWCAM[Preview camera image]
  GALLERY --> PREVIEWGAL[Preview gallery image]
  GEO --> PREVIEWGEO[Preview geo location]

  %% All previews lead to sending attachment
  PREVIEWCAM --> SENDATTACH;
  PREVIEWGAL --> SENDATTACH;
  PREVIEWGEO --> SENDATTACH;

  SENDATTACH["User clicks 'Send' for attachment"]
      --> LOCALSYNC2[Attachment synced in local storage]
      --> SENDSERVER2[Attachment sent to server]
      --> CHECKATTACH{Was attachment sent successfully?}
  CHECKATTACH -- Yes --> ATTACHSUCCESS[Attachment delivered]
  CHECKATTACH -- No --> RETRYATTACH[Prompt user to retry sending attachment]
```

### UATs

## Positive Tests

### Test 1: Create New Account

**Input Conditions**
- User has an internet connection.
- User provides a valid email, username, and password.
- Password meets security requirements (e.g., length, special characters).

**Output Conditions**
- User receives a success message.
- User is redirected to the chat list page.

**Steps**
1. Open the app and navigate to the registration page.
2. Enter a valid email, username, and password.
3. Click the "Continue" button.
4. Add profile description and avatar.
5. Click "Sign Up".

**Result**: PASS/FAIL

---

### Test 2: Send Message with Geolocation

**Input Conditions**
- User has an internet connection.
- User is logged in.
- User has a chat with another user.

**Output Conditions**
- Message with geolocation is sent.

**Steps**
1. Open the app and navigate to an existing chat.
2. Click the "Attachments" button.
3. Click the "Geolocation" button.
4. Allow location permissions if prompted.
5. Click the "Send" button.

**Result**: PASS/FAIL

---

### Test 3: Create New Chat

**Input Conditions**
- User has an internet connection.
- User is logged in.
- User knows another username.

**Output Conditions**
- A new chat is created.
- Both users can see the chat.

**Steps**
1. Open the app and navigate to the chat list page.
2. Click the “+” button at the bottom.
3. Type an existing username in the search field.
4. Choose the user.
5. Write a message and click send.

**Result**: PASS/FAIL

---

### Test 4: Change User Avatar

**Input Conditions**
- User has an internet connection.
- User is logged in.

**Output Conditions**
- User sees a new avatar.

**Steps**
1. Open the app and navigate to the settings page.
2. Click "Change Avatar".
3. Choose a photo from the gallery or take a new photo.
4. Cut it as desired.
5. Click "Confirm".

**Result**: PASS/FAIL

---

### Test 5: Set Up 2FA with Google Authenticator

**Input Conditions**
- User has an internet connection.
- User is logged in.
- User has Google Authenticator.

**Output Conditions**
- User sees a success message for 2FA setup.

**Steps**
1. Open the app and navigate to the settings page.
2. Click "Setup 2FA" button.
3. Copy the code from the screen.
4. Go to the Google Authenticator app and paste the copied code.
5. Enter additional info for Google Authenticator.
6. Enter an OTP code from Google Authenticator into the app.

**Result**: PASS/FAIL

---

### Test 6: Mute Chat Notifications

**Input Conditions**
- User has an internet connection.
- User is logged in.
- User has at least one active chat.

**Output Conditions**
- Notifications for the muted chat are disabled.

**Steps**
1. Open the app and navigate to the chat list.
2. Long press on a chat to reveal options.
3. Select the "Mute Notifications" option.
4. Choose a mute duration (e.g., 1 hour, 8 hours, or indefinitely).
5. Verify that the chat now shows a muted icon, and notifications are not received.

**Result**: PASS/FAIL

---

### Test 7: Delete a Chat

**Input Conditions**
- User has an internet connection.
- User is logged in.
- User has at least one chat in the chat list.

**Output Conditions**
- The selected chat is removed from the chat list.

**Steps**
1. Open the app and navigate to the chat list.
2. Long press on the chat to reveal options.
3. Select "Delete" from the available options.
4. Confirm the deletion in the popup.
5. Check that the chat no longer appears in the list.

**Result**: PASS/FAIL

---

## Negative Tests

### Test 8: Create Account with Invalid Email

**Input Conditions**
- User has an internet connection.
- User enters an incorrectly formatted email (e.g., "userexample.com").

**Output Conditions**
- The app displays an error message: "Invalid email format."
- The account is not created and the user is not redirected.

**Steps**
1. Open the app and navigate to the registration page.
2. Enter a valid username and password.
3. Enter an invalid email (e.g., "usernamedomain").
4. Click the "Sign Up" button.
5. Read the error message displayed.

**Result**: PASS/FAIL

---

### Test 9: Change User Avatar with Too Large File (1MB+)

**Input Conditions**
- User has an internet connection.
- User is logged in.

**Output Conditions**
- User sees an error message telling that the inserted file is too big.

**Steps**
1. Open the app and navigate to the settings page.
2. Click "Change Avatar".
3. Choose a photo from the gallery that is too large.
4. Cut it as desired.
5. Click "Confirm".

**Result**: PASS/FAIL

---

### Test 10: Send Message Without Internet

**Input Conditions**
- User has no internet connection.
- User is logged in.
- User has an existing chat.

**Output Conditions**
- The app displays an error message: "No internet connection. Message not sent."
- The message is not delivered.

**Steps**
1. Open the app and navigate to an existing chat.
2. Turn off the internet connection (Wi-Fi and mobile data).
3. Type a message in the input field.
4. Click the "Send" button.
5. Observe the error message and verify that the message remains unsent.

**Result**: PASS/FAIL


## Design

<iframe
    className="card !p-0 overflow-hidden w-full aspect-[4/3]"
    src="https://embed.figma.com/design/hJzlOgeBylo7UBcMWTZafD/MTAA-2025?node-id=12-40&embed-host=share"
    allowFullScreen>
</iframe>

<Card
    title="Link to Figma"
    className="mt-4"
    href="https://www.figma.com/design/hJzlOgeBylo7UBcMWTZafD/MTAA-2025?node-id=12-40&t=JTjvtUcyg47imjuV-1"
/>
